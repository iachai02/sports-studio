[project]
name = "sports-studio"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["core", "api", "uvicorn"]

[tool.uv.sources]
core = { workspace = true }
api = { workspace = true }

[tool.uv.workspace]
members = ["packages/*"]

[dependency-groups]
dev = ["pytest>=8.0.0", "httpx>=0.27.0"]
