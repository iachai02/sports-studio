[project]
name = "sports-studio"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["core", "api"]

[tool.uv.sources]
core = { workspace = true }
api = { workspace = true }

[tool.uv.workspace]
members = ["packages/*"]
